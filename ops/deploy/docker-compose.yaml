services:
  home-go:
    image: ghcr.io/azarakovskiy/home-go:master
    restart: unless-stopped
    env_file: .env
    ports:
      - "${HTTP_PORT:-8080}:8080"
    environment:
      DATABASE_URL: "${DATABASE_URL}"
